# Mapping specs: one per regulator per schema version, pure data consumed
# by regulator_formats/engine.py. Specs are the wire-format analogue of
# includes/jurisdictions.js — market variance as data, versioned with the
# regulator schema they target (see docs/regulator/translation-architecture.md).
